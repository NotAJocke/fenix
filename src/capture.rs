use crate::{
    action::{Action, actions_from},
    board::{Board, Coord},
    game::Game,
};

pub fn capture_options(board: &Board) -> Vec<Action> {
    todo!()
}

pub fn capture_options_from(board: &Board, from: Coord) -> Vec<Action> {
    todo!()
}
